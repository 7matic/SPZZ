version: '3.8'

services:
  mysql:
    container_name: mysql
    image: mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    ports:
      - 3306:3306
    volumes:
      - mysql-data:/var/lib/mysql

  phpmyadmin:
    container_name: phpmyadmin
    image: phpmyadmin/phpmyadmin
    ports:
      - 8080:80
    environment:
      PMA_HOST: mysql
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    depends_on:
      - mysql

  cv-parser:
    container_name: cv-parser
    build:
      context: ./cv-parser
      dockerfile: Dockerfile.dev
    ports:
      - 5000:5000
    volumes:
      - ./cv-parser:/code
      - ./cv-parser/pdfs:/code/pdfs
      - shared-data:/code/pdfs:rw

  sveltekit:
    container_name: sveltekit
    build: 
      context: ./sveltekit
      dockerfile: Dockerfile.dev
    ports:
      - 5173:5173
    volumes:
      - ./sveltekit:/app
      - /app/node_modules
    environment:
      NODE_ENV: development

  express:
    container_name: express
    build:
      context: ./express
      dockerfile: Dockerfile.dev
    ports:
      - 3000:3000
    volumes:
      - ./express:/code
      - /app/node_modules
      - shared-data:/code/pdfs:rw
    environment:
      NODE_ENV: development
    depends_on:
      mysql:
        condition: service_started

volumes:
  mysql-data:
  shared-data: